import pytest
import numpy as np

import sgpykit.src.detect_insufficient_tolerance


@pytest.mark.parametrize(
    "pts,tol,expected",
    [
        # TODO: fill in test data for test_detect_insufficient_tolerance
        # pytest.param(, , id="")
    ]
)
def test_detect_insufficient_tolerance(pts, tol, expected):
    # TODO: create assertions for test_detect_insufficient_tolerance
    # sgpykit.src.detect_insufficient_tolerance.detect_insufficient_tolerance(pts, tol)
    pass