import pytest
import numpy as np

import sgpykit.tools.polynomials_functions.lagr_eval


@pytest.mark.parametrize(
    "current_knot,other_knots,non_grid_points,expected",
    [
        # TODO: fill in test data for test_lagr_eval
        # pytest.param(, , , id="")
    ]
)
def test_lagr_eval(current_knot, other_knots, non_grid_points, expected):
    # TODO: create assertions for test_lagr_eval
    # sgpykit.tools.polynomials_functions.lagr_eval.lagr_eval(current_knot, other_knots, non_grid_points)
    pass